export interface VehicleSpec {
  label: string;
  value: string;
}

export interface FeatureSection {
  title: string;
  subtitle: string;
  images: string[];
  descriptions: string[];
}

export interface ColorOption {
  id: string;
  name: string;
  color: string;
}